const GITHUB_API = "https://api.github.com";

const githubLinks = {
  create: `${GITHUB_API}/user/repos`,
  fetchAll: `${GITHUB_API}/user/repos`,
  fetchUser: `${GITHUB_API}/user`,
};
export default githubLinks;
